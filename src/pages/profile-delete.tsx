import { View } from 'react-native';
import { Text } from 'react-native-paper';

const ProfileDeletePage = () => {
  return (
    <View>
      <Text>User delete page</Text>
    </View>
  );
};

export default ProfileDeletePage;
